program max {
    declare a,b,c,d,e;
    
    function max(in x, in y) {
        if (x>y)
            return(x);
        else
            return(y);
    }
    
    a := 1;
    b := 2;
    c := 3;
    d := 42;
    e:=max(in max(in a, in b),
           in max(in c, in d));

    print(e);
}.
